@client{
  js{
    function add(a, b = 1) { return a + b; }
    const r1 = add(2, 3);
    const r2 = add(2);
    console.log("add:", r1, r2);

    const f = (x = 10, ...rest) => { return x; };
    console.log("arrow:", f(5), f());
  };
}

